extends layout

block content
  link(rel="stylesheet",href="/stylesheets/todo_main.css")

  section.todo_main
    h1(id="main_title") MY TODO LIST

    form(method="POST")
      input(type="text",name="todo", id="todo",placeholder="하고싶은일")

      button(type="button", id="btn_save") 저장

    article.todo_list
      button(id="btn-new") 새로고침
      ul#todo_list
